#include <stdio.h>

void incrementar(int *p) {
    (*p)++;
}

int main() {
    int num = 10;

    printf("Antes do incremento: %d\n", num);
    incrementar(&num);
    printf("Depois do incremento: %d\n", num);

    return 0;
}
